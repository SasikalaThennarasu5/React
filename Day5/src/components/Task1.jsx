function ListRenderingIntro() {
  return (
    <p>
      In React, lists are rendered using the <code>map()</code> method. It allows
      developers to transform arrays into dynamic JSX elements.
    </p>
  );
}
export default ListRenderingIntro