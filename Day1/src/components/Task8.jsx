import React from 'react';
import ReactDOM from 'react-dom/client';

function Task8() {
  return (
    <div>
      <h3>8. ReactDOM.render()</h3>
      <p>This method is now deprecated. React 18 uses <code>ReactDOM.createRoot</code> instead.</p>
    </div>
  );
}

export default Task8;
