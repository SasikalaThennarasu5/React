import React from 'react';

const Task1 = () => {
  return (
    <div>
      <h2>Task 1: Introduction to Context API</h2>
      <p>The Context API in React provides a way to pass data through the component tree without having to pass props down manually at every level. It is used to share values like themes, user information, or settings between components, especially when they are deeply nested.</p>
    </div>
  );
};

export default Task1;
